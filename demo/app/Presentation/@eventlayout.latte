<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{ifset title}{include title|stripHtml} | {/ifset}Nette Web</title>
    <style>
        body, html {
            margin: 0;
            padding: 0;
            font-family: Arial, sans-serif;
            height: 100%;
        }

        /* Top Menu Styling */
        .navbar {
            background-color: #00008B; /* Dark Blue */
            padding: 10px 20px;
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .navbar a {
            color: white;
            text-decoration: none;
            padding: 8px 15px;
            border-radius: 4px;
        }

        /* The Highlighted Item */
        .navbar a.active {
            background-color: #4682B4; /* Lighter blue highlight */
            font-weight: bold;
        }

        /* Hover effect for non-active links */
        .navbar a:hover:not(.active) {
            background-color: rgba(255, 255, 255, 0.1);
        }

        /* Main Content Styling */
        .content {
            background-color: #D3D3D3; /* Light Gray */
            min-height: calc(100vh - 50px); 
            padding: 40px;
            box-sizing: border-box;
            overflow-y: auto;
        }

        .flash {
            padding: 10px;
            margin-bottom: 20px;
            border-radius: 4px;
        }
        .flash.msg_success { background: #d4edda; color: #155724; border: 1px solid #c3e6cb; }
        .flash.msg_error { background: #f8d7da; color: #721c24; border: 1px solid #f5c6cb; }
    </style>
</head>

<body>
    <nav class="navbar">
        <a n:href=":Event:default" n:class="$presenter->isLinkCurrent(':Event:default') ? active">Events</a>
        <a n:href=":Notification:default" n:class="$presenter->isLinkCurrent(':Notification:default') ? active">Notifications</a>
        <a n:href=":Notification:toApprove" n:class="$presenter->isLinkCurrent(':Notification:toApprove') ? active">To Approve</a>
        <a n:href=":Notification:scheduled" n:class="$presenter->isLinkCurrent(':Notification:scheduled') ? active">Scheduled</a>
    </nav>

    <main class="content">
        <div n:foreach="$flashes as $flash" n:class="flash, $flash->type">{$flash->message}</div>
        {include content}
    </main>

	{block scripts}
	<script src="https://unpkg.com/nette-forms@3"></script>
	{/block}
</body>
</html>