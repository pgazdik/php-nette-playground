{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Docker",
            "type": "php",
            "request": "launch",
            "port": 9003,
            // Server Remote Path -> Local Project Path
            "pathMappings": {
                "/application/demo": "${workspaceRoot}/demo"
            },
            "hostname": "localhost",
            "xdebugSettings": {
                "max_data": 65535,
                "show_hidden": 1
            }
        }
    ]
}